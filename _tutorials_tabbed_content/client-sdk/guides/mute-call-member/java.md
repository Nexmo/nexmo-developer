---
title: Java
language: java
---

```java
// Mute member
callMember.mute(true, muteListener);

// Mute my member
call.getMyCallMember().mute(true, muteListener);

// Mute whole call
call.mute(true);
```
