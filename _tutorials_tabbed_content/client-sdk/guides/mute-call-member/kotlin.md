---
title: Kotlin
language: kotlin
---

```kotlin
// Mute member
callMember.mute(true, muteListener)

// Mute my member
call?.myCallMember?.mute(true, muteListener)

// Mute whole call
call?.mute(true)
```
